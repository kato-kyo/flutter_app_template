# アクティブコンテキスト: Routine Manager (YYYY-MM-DD)

## 1. 現在のフォーカス

- プロジェクトセットアップ (`F000`) を実施中。
    - `T001`: 初期構成 ✅ 完了
    - `T002`: 基本パッケージ導入 ✅ 完了
    - `T003`: ディレクトリ構造整備 ✅ 完了
    - `T004`: ローカルDB(Hive)初期設定 🔄 計画中
    - `T005`: CI/CD構築 (MVP外)
    - `T006`: 結合テスト 🔄 計画中
- 次のタスク `F000-T004` (ローカルDB(Hive)初期設定) の準備。

## 2. 最近の変更点

- Flutterプロジェクトの基本設定完了 (`F000-T001`).
- 必要な基本パッケージ (`riverpod`, `hive`, `flutter_local_notifications` 等) を導入 (`F000-T002`).
- DDD/レイヤードアーキテクチャに基づき、`lib/src` 配下にディレクトリ構造を作成し、`.gitkeep` を追加 (`F000-T003`).
- 上記タスクの変更をコミットし、プルリクエストを作成。(#2: `feature/f000-t003-setup-directories`)

## 3. 次のステップ

- プルリクエスト #2 のレビューとマージ待ち.
- ベースブランチ (`feature/f000-base`) から新しいタスクブランチ (`feature/f000-t004-setup-hive`) を作成する.
- タスク `F000-T004` (ローカルDB(Hive)初期設定) を開始する.
    - Hiveの初期化処理を実装する.
    - Hiveアダプター生成のための設定を行う.
    - (`build_runner` の実行はデータモデル定義後)

## 4. アクティブな決定事項と考慮事項

- **ローカルDB:** Hive を採用確定。`hive_flutter` パッケージも導入済み.
- **状態管理:** Riverpod を採用確定.
- **アチーブメント:** MVPではシンプルな条件と通知、一覧表示を実装。デザインは最低限とする.
- **テスト:** 各機能の実装と並行して単体テスト・ウィジェットテストを記述する.
- **ルーティング:** GoRouterの導入を検討中（タスク管理機能実装時に具体化）.

## 5. 重要なパターンと設定

- **ディレクトリ構造:** `.cursor/rules/021-directory-structure.mdc` に従い実装済み.
- **Git:** `.cursor/rules/005-git-and-github-rules.mdc` に従い運用中.
- **コーディングスタイル:** Flutter 標準 + プロジェクト固有のルール（未定義）.
- **ビルド:** `build_runner` を Hive アダプター生成に利用予定.

## 6. 学習と洞察

- `gh pr create` コマンドで本文を直接渡す際、シェルの解釈によるエラーが発生することがある。一時ファイルを使用する方が確実.
- プルリクエスト作成前に `git push` が必要.
- コミットメッセージ規約の遵守が重要. 