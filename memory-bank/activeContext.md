# アクティブコンテキスト: Routine Manager (2024-04-06)

## 1. 現在のフォーカス

- プロジェクトセットアップ (`F000`) が完了。
    - `T01`: 初期構成 ✅ 完了
    - `T02`: 基本パッケージ導入 ✅ 完了
    - `T03`: ディレクトリ構造整備 ✅ 完了
    - `T04`: ローカルDB(Hive)初期設定 ✅ 完了
    - `T05`: CI/CD構築 (MVP外)
    - `T06`: 結合テスト ✅ 完了
- 次の機能 `F001` (タスク登録) の実装準備。

## 2. 最近の変更点

- Flutterプロジェクトの基本設定完了 (`F000-T01`).
- 必要な基本パッケージ (`riverpod`, `hive`, `flutter_local_notifications` 等) を導入 (`F000-T02`).
- DDD/レイヤードアーキテクチャに基づき、`lib/src` 配下にディレクトリ構造を作成し、`.gitkeep` を追加 (`F000-T03`).
- 上記タスクの変更をコミットし、プルリクエストを作成。(#2: `feature/f000-t003-setup-directories`)
- Hive の初期化処理を `lib/main.dart` に追加 (`F000-T04`).
- 上記タスク(`F000-T04`)の変更をコミットし、プルリクエストを作成。(#3: `feature/f000-t004-setup-hive`)
- 結合テスト環境をセットアップし、Androidビルド設定・VS Code設定を更新 (`F000-T06`).
- 上記タスク(`F000-T06`)の変更をコミットし、プルリクエストを更新/作成。(#4: `feature/f000-t006-integration-test`)

## 3. 次のステップ

- プルリクエスト #2, #3, #4 のレビューとマージ待ち。
- (マージ後) `develop` ブランチを作成し、`feature/f000-base` をマージする。
- `develop` ブランチから新しい機能ブランチ (`feature/f001-task-create`) を作成する。
- タスク `F001` (タスク登録) の実装を開始する。
    - データモデル (Entity, DTO, Hive Adapter) の定義。
    - Repository (インターフェース、実装) の作成。
    - Application Service / Riverpod Provider の作成。
    - UI (Page, Widget) の作成。

## 4. アクティブな決定事項と考慮事項

- **ローカルDB:** Hive を採用確定。`hive_flutter` パッケージも導入済み.
- **状態管理:** Riverpod を採用確定.
- **アチーブメント:** MVPではシンプルな条件と通知、一覧表示を実装。デザインは最低限とする.
- **テスト:** 各機能の実装と並行して単体テスト・ウィジェットテストを記述する.
- **ルーティング:** GoRouterの導入を検討中（タスク管理機能実装時に具体化）.

## 5. 重要なパターンと設定

- **ディレクトリ構造:** `.cursor/rules/021-directory-structure.mdc` に従い実装済み.
- **Git:** `.cursor/rules/005-git-and-github-rules.mdc` に従い運用中.
- **コーディングスタイル:** Flutter 標準 + プロジェクト固有のルール（未定義）.
- **ビルド:** `build_runner` を Hive アダプター生成に利用予定.

## 6. 学習と洞察

- `gh pr create` コマンドで本文を渡す際、シェルの解釈によるエラーを避けるためには `--body-file` オプションで一時ファイルを渡すのが確実。
- プルリクエスト作成前に `git push` が必要。
- コミットメッセージ規約の遵守が重要。
- Android結合テスト実行には NDK バージョンと Core Library Desugaring の設定が必要な場合がある。
- **ワークフロー:** プルリクエストの作成/編集前には、必ずメモリバンクの確認・更新を行うことがユーザー様から求められている。 
