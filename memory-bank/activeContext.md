# アクティブコンテキスト: Routine Manager (YYYY-MM-DD)

## 1. 現在のフォーカス

- 開発タスク `F000` (プロジェクトセットアップ) 進行中。
- 次のタスク: `T002` (基本パッケージ導入)。

## 2. 最近の変更点

- タスク `F000-T001` (初期構成) を完了済みとしてマーク。
- `feature/f000-t001-initial-setup` ブランチから `feature/f000-base` へプルリクエストを作成。
- メモリバンクの初期ファイルを作成しコミット。

## 3. 次のステップ

- プルリクエストのレビューとマージを待つ。
- マージ後、`feature/f000-base` から `feature/f000-t002-install-packages` ブランチを作成する。
- タスク `T002` (基本パッケージ導入) を開始する。
    - `pubspec.yaml` に Riverpod, Hive, flutter_local_notifications 等を追加。
    - `flutter pub get` を実行。

## 4. アクティブな決定事項と考慮事項

- **ローカルDB:** Hive を第一候補とするが、実装のしやすさによっては SharedPreferences や sqflite も検討の余地あり。
- **状態管理:** Riverpod を採用。
- **アチーブメント:** MVPではシンプルな条件と通知、一覧表示を実装。デザインは最低限とする。
- **テスト:** 各機能の実装と並行して単体テスト・ウィジェットテストを記述する。

## 5. 重要なパターンと設定

- **ディレクトリ構造:** `.cursor/rules/021-directory-structure.mdc` に従う。
- **Git:** `.cursor/rules/005-git-and-github-rules.mdc` に従う。
- **コーディングスタイル:** Flutter 標準 + プロジェクト固有のルール（未定義）。

## 6. 学習と洞察

- `gh pr create` コマンドはリモートリポジトリが設定されていないと失敗する。
- 複数行の本文を `gh pr create --body-file` で渡す方が確実。
- MVPスコープは、ユーザー価値と開発期間のバランスを考慮して柔軟に見直すことが重要。
- ゲーミフィケーション要素は、モチベーション向上に寄与する可能性がある一方で、実装コストと複雑さを増すトレードオフがある。 